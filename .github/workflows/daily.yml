name: Daily PFF Holdings Scrape

on:
  schedule:
    - cron: "0 11 * * 2-5" # 6 am ET (11:00 UTC) Tue–Fri, captures Mon–Thu data
    - cron: "0 20 * * 6" # 3 pm ET (20:00 UTC) Saturday, captures Friday late-night upload
  workflow_dispatch:
    inputs:
      date:
        description: "Date to backfill (YYYYMMDD). Leave blank for today."
        required: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pipeline
        working-directory: scripts
        env:
          OPENFIGI_API_KEY: ${{ secrets.OPENFIGI_API_KEY }}
        run: python run_pipeline.py ${{ github.event.inputs.date }}

      - name: Commit and push data
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add data/PFF/ data/PGX/ data/FPE/ data/PFFA/ data/PFFD/ data/PFXF/ data/ticker_cache.json data/ticker_overrides.json data/predicted_flows.csv data/daily_summary.json data/ticker_summary.json data/overlap_summary.json data/adv_cache.json
          git diff --staged --quiet && echo "No changes to commit." || \
            git commit -m "data: PFF holdings $(date -u +%Y-%m-%d)"
          git push
